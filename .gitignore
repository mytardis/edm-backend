# App artifacts
/_build
/db
/deps
/*.ez

# Mix lock file
mix.lock

# Generate on crash by the VM
erl_crash.dump

# Static artifacts
/node_modules

# Since we are building assets from web/static,
# we ignore priv/static. You may want to comment
# this depending on your deployment strategy.
/priv/static/

# The config/*.secret.exs file by default contains sensitive
# data and you should not commit it into version control.

/config/*.secret.exs

# Ignore iml files and the .idea directory for jetbrains IDEs
**/.idea/
*.iml